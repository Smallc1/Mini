"use client";

export default function ConfirmDialog({ open, title = "操作提示", message, pending = false, confirmLabel = "确认", onCancel, onConfirm }: { open: boolean; title?: string; message: string; pending?: boolean; confirmLabel?: string; onCancel: () => void; onConfirm: () => void }) {
  if (!open) return null;
  return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950/45 p-4" onMouseDown={(event) => { if (event.target === event.currentTarget && !pending) onCancel(); }}><div role="alertdialog" aria-modal="true" className="w-full max-w-md overflow-hidden rounded-2xl border border-amber-300 bg-amber-50 shadow-2xl"><div className="flex gap-3 p-6"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-amber-400 text-xl text-amber-950">⚠</span><div><h3 className="text-lg font-semibold text-amber-950">{title}</h3><p className="mt-2 whitespace-pre-line text-sm leading-6 text-amber-900/80">{message}</p></div></div><div className="flex justify-end gap-2 border-t border-amber-200 bg-amber-100/70 px-6 py-4"><button type="button" disabled={pending} onClick={onCancel} className="rounded-lg border border-amber-300 bg-white px-4 py-2 text-sm font-medium text-slate-600 hover:bg-amber-50 disabled:opacity-50">取消</button><button type="button" disabled={pending} onClick={onConfirm} className="rounded-lg bg-amber-500 px-4 py-2 text-sm font-semibold text-amber-950 hover:bg-amber-600 disabled:opacity-50">{pending ? "处理中..." : confirmLabel}</button></div></div></div>;
}
